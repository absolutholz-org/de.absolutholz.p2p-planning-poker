import { Meta, Typeset } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Typography" />

# Typography

Our typography system is built on the **Inter** font family, prioritizing legibility and a modern aesthetic.

**Font Family:** `var(--sys-font-family)`

**Monospace Family:** `var(--sys-font-mono)`

## 🧱 Weights

We use a few specific weights for visual hierarchy.

- **Normal:** `var(--sys-font-weight-normal)` (400)
- **Medium:** `var(--sys-font-weight-medium)` (500)
- **Bold:** `var(--sys-font-weight-bold)` (700)

## 📏 Font Sizes

Available as `--sys-font-size-*` CSS variables.

<Typeset
  fontSizes={[
    'var(--sys-font-size-xs)',
    'var(--sys-font-size-sm)',
    'var(--sys-font-size-md)',
    'var(--sys-font-size-lg)',
    'var(--sys-font-size-xl)',
    'var(--sys-font-size-2xl)',
    'var(--sys-font-size-3xl)',
  ]}
  fontWeight={400}
  sampleText="The quick brown fox jumps over the lazy dog"
  fontFamily="var(--sys-font-family)"
/>

### Monospace

<Typeset
  fontSizes={[
    'var(--sys-font-size-sm)',
    'var(--sys-font-size-md)',
    'var(--sys-font-size-lg)',
    'var(--sys-font-size-xl)',
  ]}
  fontWeight={400}
  sampleText="01:23:45"
  fontFamily="var(--sys-font-mono)"
/>

## 📝 Usage

- **XS (12px):** Labels, captions, micro-copy.
- **SM (14px):** Secondary text, small buttons.
- **MD (16px):** Body text, default inputs.
- **LG (18px):** Navigation elements, tooltips.
- **XL (20px):** Sub-headings, active labels.
- **2XL (24px):** Page titles, primary headings.
- **3XL (36px):** Large display titles.
